# Decisions
> ~2 min read

| # | Date | Slice | Decision | Why | Rejected Alternative |
|---|------|-------|----------|-----|----------------------|
| 1 | 2026-06-27 | — | Single integrated flow-planner session (not two sequential steps) | Avoids duplicate work and plan divergence; PCTO IS the spec input for the upgrade | Run integrate-PCTO first, then separately run flow-planner |
| 2 | 2026-06-27 | — | PCTO slices numbered 21–23 in existing project scheme | Preserves continuity with 20-slice history in PROGRESS.md | Start new numbering from 1 in a separate PCTO plan file |
| 3 | 2026-06-27 | — | Slice 21 before Slice 19 | Hackathon deadline; PCTO Slice 1 (Days 1–5) is highest-value; Atlas M0 workaround covers interim risk | Slice 19 first — correct long-term but delays hackathon deliverable |
| 4 | 2026-06-27 | 21 | `sie_embedder.py` as new file mirroring `local_embedder.py` interface | Composability — orchestrator dispatches by provider string; each provider is an independent module | Modify `embedder.py` directly — breaks single-responsibility |
| 5 | 2026-06-27 | 21 | `SweepRequest` accepts caller-supplied `corpus: list[str]`; falls back to topic string when empty | No external dependencies or API keys required; simpler, fully testable, composable | Inline corpus fetching — hidden I/O in the pipeline, harder to test |
| 6 | 2026-06-27 | 21 | `aim_logger.py` as thin wrapper (not direct `aim` calls) | Replaceable — PCTO risk table names MLflow as backup; wrapper isolates the dependency | Direct `aim.Run()` calls in orchestrator — hard to swap |
| 7 | 2026-06-27 | 21 | Voyage AI stays as numeric baseline (not removed) | PCTO spec: "voyage-3.5 stays as the numeric baseline. Every sweep run compares at least one SIE model against it." | Remove Voyage to simplify — contradicts PCTO spec |
| 8 | 2026-06-27 | — | MCP server explicitly deferred — **Won't have this cycle** | No immediate hackathon value; adds deployment complexity (Alpic.ai account, MCP protocol) without improving sweep quality. `GET /api/v1/best-config` is the clean integration point if ever needed. | Embed MCP in service (Slice 22) — couples transport concern to sweep logic; harder to reuse |
| 9 | 2026-06-27 | — | If MCP is ever built, it must be a standalone reusable skill or global tool | MCP is a transport layer, not a domain concern. Embedding it in `rag-params-finder` would prevent reuse across other services. A standalone skill can wrap any `best-config`-compatible endpoint. | Inline `mcp_server.py` in this service — tight coupling, not reusable |
| 10 | 2026-06-27 | all | When a new RAG dimension (DB/embedder/chunker/retriever) is introduced, apply the **simplest design pattern** that fits the routing/dispatch problem. Factory function preferred over Protocol/ABC unless a contract must be enforced across teams or packages. Upgrade only when complexity demands it. Applied to Slice 21: `embedder_factory.py` (factory fn, not Protocol class). | YAGNI + Simple Design — pattern overhead is a cost; ceremony without runtime benefit is waste. | Default to Protocol/facade for every new provider — adds ~40 lines with zero runtime gain at current scale |
| 11 | 2026-07-01 | 28 | Dedicated Slice 28 for results export (issue #49) instead of waiting for bundled Slice 11 | Export is a shippable vertical slice with clear acceptance criteria; unblocks external contributors and stakeholder sharing | Fold into Slice 11 Search Explorer bundle — delays delivery behind visualization work |
| 12 | 2026-07-01 | 28 | Export rows sourced from `analyze_results()` `detailed_results`, not raw Mongo `results` docs | Scores/ranks match Search Explorer; single normalization path | Duplicate CSV logic in route from raw docs — drift risk vs dashboard |
| 13 | 2026-07-01 | 28 | Default CSV omits `chunk_text`; JSONL may include it | Keeps CSV small and avoids dumping full chunk bodies into spreadsheets | Always include chunk_text — noisy for stakeholders, large files |
| 14 | 2026-07-02 | health-check | Gap 2: routing fingerprint | Appended `Routing: Brownfield + Growing Requirement (Flow D)` to TRAIL.md | AUTO-FIXED |
| 15 | 2026-07-02 | health-check | Gap 4: Depends on column | Added to TRAIL.md Slices table; existing rows defaulted per dependency graph | AUTO-FIXED |
| 16 | 2026-07-02 | health-check | Gap 5: gate-evidence stubs | Backfilled slice-21.json, slice-25.json, slice-25B.json | AUTO-FIXED |
| 17 | 2026-07-02 | health-check | Gap 1: interview_summary.md | Reconstructed from TRAIL + PROGRESS + PCTO spec | AUTO-FIXED |
| 18 | 2026-07-02 | health-check | Gap 3: model split | Defaults: Planning claude-opus-4-8 · Execution claude-sonnet-4-6 | AUTO-FIXED |
| 19 | 2026-07-02 | — | Created docs/plan/PROGRESS.md | plan-modifier requires plan-level tracker separate from docs/plan/slices/PROGRESS.md | **Superseded by #23** |
| 23 | 2026-07-02 | — | Merge docs/plan/PROGRESS.md into docs/plan/slices/PROGRESS.md | Single SSOT — AGENTS.md/CLAUDE.md already point to slices/PROGRESS; duplicate caused drift | Delete docs/plan/PROGRESS.md |
| 20 | 2026-07-02 | — | Dependabot #26–#43 triage recorded | 4 merged (#36–#39 CI actions), 5 closed (breaking toolchain bumps) | Execute when stale |
| 21 | 2026-07-02 | — | Deferred toolchain upgrades documented in TRAIL + GAP_ANALYSIS | Vite 8, ESLint 9, react-hooks 7, ST v4+ need dedicated slices — not blind Dependabot merges | Close-and-document pattern |
| 22 | 2026-07-02 | — | GAP_ANALYSIS refresh cadence | Update on slice PASS or quarterly; stale analysis misleads continuation sessions | Manual until automated |
| 24 | 2026-07-04 | health-check | PRs #56, #57 merged; PR Queue updated | Actions/cache v6 + actions/checkout v7 merged to main; removed from PROGRESS.md open queue | AUTO-FIXED |
| 25 | 2026-07-04 | health-check | PRs #47, #48 flagged as implementation prerequisites | Chunker fixes should land before implementation slices (28, 22) touching sweep pipeline | Mark "Merge when ready" rows with explicit prerequisite note |
| 26 | 2026-07-05 | — | Plan health ✅ OK (0 gaps) — no structural changes this session | All 7 audit checks pass; staleness fixes applied; structural options (execution order, Slice 11 tracking) presented to user | Deferred pending user input |
| 27 | 2026-07-05 | T1+C2 | Reorder execution: 28 (Must) and 22 (PCTO Critical) before 26/27 (Should) | Slice 28 has no hard dep on 26/27; Slice 22 depends only on 21 (done). Must before Should; PCTO Critical before QoL | Keep original order — lets Should items accumulate before shipping Must |
| 28 | 2026-07-05 | T2 | PRs #47/#48 are hard prerequisites for Slices 28 and 22 | Chunker pipeline must be stable (overlap + padding) before export tests and SIE sweep tests exercise the same data path | Accept small regression risk — merge after implementation |
| 29 | 2026-07-05 | C1 | Add Slice 11 (Search Explorer enhancements) to TRAIL.md tracked slices | TRAIL and PROGRESS were diverged; Slice 11 has a spec entry and Forward Roadmap row but was absent from TRAIL. Could / no hard dep | Leave in Forward Roadmap only — defer tracking until after Slice 23 |
| 30 | 2026-07-06 | 28/22 | Prereqs #47/#48/#59 satisfied — Slice 28 and 22 unblocked | PRs merged to main (#47 overlap, #48 padding, #60/#61 review follow-ups); plan artifacts synced | Keep stale "merge first" notes in HANDOFF — caused false blocker on continuation |
| 31 | 2026-07-06 | 28/22 | Slice 28 stays PLANNED — not starting immediately | Spec on main (PR #55); no implementation branch yet; active work begins at Slice 22; do not mark DEFERRED | Mark DEFERRED — mislabels a queued Must slice as out of scope |
| 32 | 2026-07-06 | 28 | Slice 28 owned by issue #49 author @cschanhniem | External contributor implements export; core team focuses on Slice 22; issue author is assignee on GitHub | Core team implements Slice 28 — duplicates contributor intent on #49 |
| 33 | 2026-07-07 | health-check | Gap 8: coverage gates missing from 9 PLANNED slice stubs | USER-CONFIRMED: added spec coverage + branch coverage + mutation testing checks to SLICE-22/23/28/26/27/16/19/10 After-Checks blocks | Skip — add per-slice at execution time |
| 34 | 2026-07-07 | planning | Added Slice 30: Search Explorer UX fixes | Assessment found 4 untracked UX issues (tab latency, zero-score noise, BM25 labels, VDB card): bundled as Could / ~2h, no deps | Fold into Slice 11 — Slice 11 is scoped to viz+filtering; UX fixes are distinct enough to warrant their own scope |
| 35 | 2026-07-07 | planning | Added Slice 31: Experiment list filter | Assessment found navigability gap at scale (no status filter, no search); Should / ~2h, no deps | Leave untracked — small enough for quick fix without a slice |
| 36 | 2026-07-07 | planning | Added Slice 29 row to TRAIL.md | Slice 29 (Padding Propagation) was ✅ COMPLETE in PROGRESS.md but absent from TRAIL.md formal tracking table — tracking gap, not a new slice | Leave in PROGRESS.md only |
| 37 | 2026-07-07 | 22 | Reclassified Slice 22 from Should → Must | nw-review NEEDS_REVISION: Slice 22 delivers PCTO-critical SIE score/reranking + `GET /api/v1/best-config`; Slice 21 (encode) alone does not satisfy the PCTO spec. Both halves are Must. | Keep as Should — incorrect: incomplete PCTO spec without it |
| 38 | 2026-07-07 | 30 | Rewrote SLICE-30 ACs to remove implementation prescriptions | nw-review NEEDS_REVISION (blocking): ACs must describe observable behavior (WHAT), not implementation detail (HOW). Removed `useTransition`, `opacity-40`, and polling-timer internal from ACs; replaced with timing budgets and visual outcome statements | Keep implementation prescriptions in ACs — violates behavioral AC principle |
| 39 | 2026-07-09 | health-check | Gap 5: gate-evidence for Slice 29 | Backfilled `docs/plan/gate-evidence/slice-29.json` (PASSED in TRAIL, evidence missing) | AUTO-FIXED |
| 40 | 2026-07-09 | planning | Dual-backend storage Protocol (Mongo + Postgres), not in-place Mongo delete | PRD §8 open decision; rollback + side-by-side quality comparison required before cutover | Full replace only — loses A/B and rollback |
| 41 | 2026-07-09 | planning | Prioritize migration slices 32–38 ahead of Slice 22 | User confirmed; storage foundation before more SIE/history work on Mongo-shaped APIs | Keep 22 first — PCTO Must before store swap |
| 42 | 2026-07-09 | planning | Added slices 32–38 skateboard chain for Supabase/pgvector | Optimal vertical slices: Protocol → CRUD → dense → sparse/hybrid → preflight/stats → local/cloud → ADR cutover | Single epic slice — too large for hard gates |
| 43 | 2026-07-09 | planning | Deferred slices 26, 27, 19 (Mongo QoL / Atlas quota) | Postgres becomes primary; Mongo-local docs and Atlas quota guard are low value until post-cutover re-scope | Keep 26/27/19 in active execution order |
| 44 | 2026-07-09 | 22 | Slice 22 Depends on 21 + 38 | best-config/history and SPLADE paths should land on cut-over storage; Protocol allows earlier 22 if deadline forces | Soft dep only — risk of double-porting history queries |
| 45 | 2026-07-09 | skill-proposer | Keep model split defaults; propose /tdd /verify-slice /architecture /security-review for 32–38 | Brownfield dual-backend + secrets in DATABASE_URL; no new UI design system | Change models — user can override next session |
| 46 | 2026-07-09 | review | nw-solution-architect-reviewer: conditionally approved | Applied behavioral AC (32), equivalence gates (35/38), Protocol dep (22), PRD SSOT | Start Slice 32 without edits |
| 47 | 2026-07-09 | 32 | StorageBackend + RetrieverBackend seam locked | CRUD vs retrieval query APIs differ; orchestrator uses ports only | Single monolithic Protocol |
| 48 | 2026-07-09 | 33 | Keep external string experiment_id | No breaking API/CLI/dashboard ID change when adding Postgres PK | UUID-only external IDs |
| 49 | 2026-07-09 | 35 | SPLADE fallback: tsvector path if non-zeros > 1000 | sparsevec ceiling is hard Postgres limit | Block SPLADE on Postgres |
| 50 | 2026-07-09 | 36 | Slice 27 (mode indicator) absorbed into 36 | Storage mode during migration (taxonomy refined 2026-07-26 → four-value flag-aligned names) | Keep 27 as separate deferred slice |
| 51 | 2026-07-09 | 22 | PCTO escape hatch in TRAIL | If 32–36 slip, start 22 on Protocol after 32; retest after 38 | Hard block 22 until 38 always |
| 52 | 2026-07-09 | 38 | Cutover gates: latency ≤2× Mongo p99; hybrid drift ≤5%; equivalence ≥80% top-3 | Platform review blocker — operators need explicit PASS/FAIL before default flip | Qualitative comparison only |
| 53 | 2026-07-09 | 33–37 | Mandatory Postgres CI job before merging storage slices | Dual-backend regression; prevent Postgres path bitrot | Optional local gate only at Slice 38 |
| 54 | 2026-07-09 | 38 | Rollback: revert to mongo if incident recovery >30 min | Platform review rollback playbook | No documented rollback |
| 55 | 2026-07-09 | health-check | Continuation audit: legacy gaps 1–7 OK | Gap 8 N/A — specs live under `docs/plan/slices/` not `docs/plan/slice-*.md` | Require duplicate stubs in docs/plan |
| 56 | 2026-07-09 | 11 | Created SLICE-11-SEARCH-EXPLORER.md | TRAIL linked missing file; scope = viz + query filter only (export→28, UX→30) | Fold into Slice 30 |
| 57 | 2026-07-09 | 19/26/27 | Spec status → DEFERRED aligned with TRAIL | 27 superseded by 36; 19/26 re-scope post-cutover | Keep PLANNED in specs |
| 58 | 2026-07-09 | 10/16/22/23/28 | Bridge Before-Checks + deps + execution order | 10 PARTIAL in TRAIL; 22 hard dep 32; 28 order matches migration-first | Leave stale pre-2026-07-09 order in specs |
| 59 | 2026-07-09 | 32–38 | PRD Documentation matrix + per-slice doc gates | User/dev setup docs staged by slice; `/sync-docs` at 37/38 | Abbreviated Doc audit lines only |
| 60 | 2026-07-09 | nw-review | Iter 3 follow-ups applied post gap-bridge | Soft dep 30 (TRAIL/SLICE-11); escape-hatch >2d threshold; PRD cutover baseline snapshot; SLICE-36 Slice 27 storage-mode tests | Defer all review notes to next PR |
| 61 | 2026-07-18 | 39 | Add a ≤2 h demo-ready dashboard polish slice before Slice 32 | User prioritised an impressive UI for the imminent demo; visual-only list-to-detail work has higher immediate presentation value than migration foundations and resumes the approved migration sequence afterward | Start Slice 32 immediately — strategically correct but produces little visible demo improvement within the available time |
| 62 | 2026-07-18 | 39 | Adopt ARC-AGI-3 results storytelling principles, not its domain or implementation | Thesis-first framing, evidence progression, traceability, and progressive disclosure strengthen the existing RAG demo without depending on unpublished source or inventing capabilities; charts/search remain owned by Slices 11/30/31 | Copy the external site's structure and analytics — scope expansion, unverifiable lineage, and regression risk inside a ≤2 h visual slice |
| 63 | 2026-07-20 | 20/40 | Merge Round 2 trigger hardening into Slice 20 | Slice 20 is now the single spec source for toolchain work; Slice 40 artifact is consolidated and removed from active scope | Keep implementation in Slice 20; track follow-up in PROGRESS/DECISIONS only |
| 64 | 2026-07-20 | 40 | Plan-slices boundary clarification | Treat `docs/plan/slices/PROGRESS.md` as the status SSOT and keep `docs/plan` as planning continuity documents (TRAIL, GAP, decisions, handoff). Slice 40 formalizes this to avoid reintroducing a `docs/slices` naming mismatch | No second status tracker under `docs/plan/PROGRESS.md` |
| 65 | 2026-07-20 | health-check | Gap 8 migration pass | Added Spec coverage + Branch + Mutation testing gates to planned active slices `SLICE-21-SIE-SKATEBOARD.md` (consistency), `SLICE-33-POSTGRES-SCHEMA-CRUD.md`, `SLICE-34-POSTGRES-DENSE-RETRIEVAL.md`, `SLICE-35-POSTGRES-SPARSE-HYBRID.md`, `SLICE-36-POSTGRES-PREFLIGHT-STATS.md`, `SLICE-37-POSTGRES-LOCAL-CLOUD-PARITY.md`, and `SLICE-38-CUTOVER-ADR-004.md`; added same 3 gates to docs-only `SLICE-40-DOCS-PLAN-SLICES-SSOT.md` | Auto-fixed for active planned slice set |
| 66 | 2026-07-20 | health-check | Gate status migration | Normalized SLICE-22 / SLICE-23 from PENDING to PLANNED | AUTO-FIXED |
| 67 | 2026-07-21 | planning | Added Slice 41A: Bayesian Search: Simple Functional | Added `SLICE-41A-BAYESIAN-SEARCH-SIMPLE-FUNCTIONAL.md` and synchronized `TRAIL.md` + `docs/plan/slices/PROGRESS.md` as planned | Auto-corrected |
| 68 | 2026-07-21 | quality-lens | 10/10 checks passed | confirmed | Added 10-item Planning Quality Lens table to `SLICE-41A`, added `docs/plan/gate-evidence/slice-41A.json`, and tagged execution backlog split (`plan` vs `/nw-execute`). |
| 69 | 2026-07-21 | 41A execution | Start Bayesian execution implementation | Added config schema split, Bayesian execution branch, API planned-count/resume guard, and docs/example updates; backend behavior now has dedicated bayesian path | in progress |
| 70 | 2026-07-22 | planning | Added Slice 42: Docker Build Optimisation | Confirmed 5 problems in existing Docker files (single-stage server.Dockerfile, wrong COPY order busting PyTorch cache, no BuildKit cache mounts, frontend vite preview runtime, no CI Docker build job); nginx:alpine chosen for frontend runtime (user confirmed — ~23MB vs ~180MB node_modules image); non-blocking CI job on first landing | Deferred — problems confirmed and costed, should fix now before Supabase migration adds more Docker surface |
| 71 | 2026-07-22 | review | Slice 42 spec: NEEDS_REVISION → revised | nw-agent-builder-reviewer + nw-platform-architect-reviewer both returned NEEDS_REVISION; 3 blocking issues resolved: (1) spike command narrowed to uv sync --frozen --no-install-project only; (2) GWT Should-10 added for nginx SPA fallback verification; (3) RUN mkdir -p /root/.npm added to frontend Dockerfile files table. 4 non-blocking refinements applied. Spec now ready to branch when 41A closes or user prioritises. | APPLIED — spec committed to docs/plan-slice-42-docker-build |
| 72 | 2026-07-24 | health-check | Gap 5 AUTO-FIX: backfilled 6 gate-evidence stubs | Slices 14, 16, 18, 20, 24, 39 were COMPLETE in TRAIL.md but lacked gate-evidence JSON files; created stubs with `inferred: true` flag per plan-health-check procedure | AUTO-FIXED |
| 73 | 2026-07-22 | planning | Added Slice 41B: Bayesian Search Advanced (PARKED) | Captures full parallelism analysis (two-layer threading, constant liar strategy ≤4 workers), categorical axis expansion (pending A2 validation), study persistence, `search_strategy: random`, and dashboard Bayesian card; parked until 41A ships and production usage evidence exists — open questions A1, A2, A4, D3, D7 must resolve first (A3 decided: separate `bayesian.parallelism` field; D6 not a 41B gate) | Merge into 41A — 41A scope is already bounded to numeric axes only; categorical + parallelism require production evidence before speccing |
| 74 | 2026-07-22 | review | Applied nw-agent-builder-reviewer + nw-solution-architect-reviewer findings to SLICE-41B | 9 findings applied: A3 resolved (bayesian.parallelism field decided), D6 decoupled (not a 41B gate), A4 time-bounded (2026-10-01), Gate Evidence Specification added for ≥90% constant-liar quality, dedup pseudocode expanded, scope note for random search (optional 41C) | Accept review findings as advisory only — current spec is adequate for PARKED status |
| 75 | 2026-07-25 | health-check | Gap 5 AUTO-FIX: slice-42 gate-evidence + historical stubs 1–9 | Slice 42 (Docker Build Optimisation) merged as PR #107 but lacked gate-evidence JSON; created `slice-42.json` (inferred: false, real PR/date). Slices 1–9 predated the gate-evidence process; created 9 inferred stubs. Total gate-evidence files: 21 | AUTO-FIXED |
| 76 | 2026-07-25 | health-check | Gap 8 DEFERRED: coverage gates on historical COMPLETE stubs | 15 already-COMPLETE slice stubs (1–9, 14, 16, 20, 24, 25, 25B, 29, 39, 41B) lack Specification coverage lines in After-Checks. No value retroactively adding gates to shipped code. All 9 active PLANNED slices (22, 32–38, 40) already have coverage gates ✅ | DEFERRED — no action required |
| 77 | 2026-07-25 | health-check | GAP_ANALYSIS.md refreshed | Updated date 2026-07-09 → 2026-07-25; added 4 rows for slices 41A, 42, project hygiene batch, and gate-evidence backfill | AUTO-FIXED |
| 78 | 2026-07-25 | continuation | /enhanced-flow-planner gap audit | Health: 6/8 clean. Critical path unchanged: 32→33→34→35→36→37→38→22. No new slices needed. 19/37 complete. Next: Slice 40 (~1h, zero deps) then branch slice/32-storage-backend-protocol. Project hygiene correctly in maintenance log, not TRAIL slices. | PLAN HEALTH CONFIRMED |
| 79 | 2026-07-25 | health-check | Gap 8 USER-CONFIRMED: coverage gates inserted in SLICE-41B and SLICE-41C | 14 already-COMPLETE stubs skipped (no retroactive value); 2 future unfinished stubs (41B PARKED, 41C PLANNED) received Specification coverage + Branch coverage + Mutation testing lines in After-Checks | USER-CONFIRMED |
| 80 | 2026-07-25 | planning | Added Slice 32B: Storage Protocol Gate Closure | Path B via /enhanced-flow-planner. Owns remaining Slice 32 After-Checks (branch coverage, mutation/waiver, full quality-gates, nw-review, gate-evidence, tracker COMPLETE). Slice 33 Depends on updated 32→32B. Critical path: 32→32B→33→… Spec: `SLICE-32B-STORAGE-PROTOCOL-GATE-CLOSURE.md` | Keep pending gates on Slice 32 only — blurs implementation vs verification and blocks a clean COMPLETE for Protocol work once PR #110 lands |
| 81 | 2026-07-25 | health-check | Gap 8 DEFERRED again (historical COMPLETE stubs) | 16 COMPLETE/historical stubs still lack Specification coverage lines; deferred (same rationale as #76). Active Slice 32/32B/33 have gates. | DEFERRED |
| 82 | 2026-07-25 | planning | Added Slice 32C: Storage Protocol Review Remediation | Path B via /enhanced-flow-planner. Owns craft/architecture nw-review BLOCKERs (checklist dedupe, index deferral, port schemas, mongo_stats split, inline-import rule). 32B stays gate-only; Depends on: 32C→32B→33. Spec: `SLICE-32C-STORAGE-PROTOCOL-REVIEW-REMEDIATION.md` | Expand 32B to absorb craft work — would make 32B large and mix SLAP levels |
| 83 | 2026-07-25 | 32C | Index seam default defer to Slice 36 (option A) | YAGNI; Slice 36 already owns Postgres preflight/stats | Add IndexBackend Protocol in 32C |
| 84 | 2026-07-25 | health-check | Gap 8: 32C includes Specification coverage; historical stubs skipped | User path: create 32C rather than expand 32B; Gap 8 for COMPLETE stubs remains deferred (#76/#81) | Retrofit all historical stubs |
| 85 | 2026-07-25 | quality-lens | 32C 9/10 provisional (check 4 N/A) | Slim Must remediation; GWT-first; composable before 32B; no speculative IndexBackend | Pending user confirm on Before-Checks defaults |
| 86 | 2026-07-25 | deps | Keep `numpy>=2,<3` + `torch>=2.6` override; drop Intel mac from uv environments | `sie-sdk` needs NumPy 2; torch 2.2 + NumPy 2.5 → `Numpy is not available` on local embeds; torch≥2.6 has no x86_64 Darwin wheels | Restore `numpy<2` — conflicts with sie-sdk |
| 87 | 2026-07-26 | 33–38 | Operator flags `--<db-type>-local\|cloud`; mode values match flags; `postgres-setup.md` SSOT; Slice 37 owns rename + hosted ensure_env + two-command switching + config↔server 422 | Symmetric grid; two-boolean parse cannot express hosted Postgres; YAML must not silently disagree with live adapter | Keep `--local`/`--postgres` as sole names; create `supabase-setup.md`; let YAML flip process backend |
| 88 | 2026-07-26 | health-check | Gap 5 AUTO-FIX: `gate-evidence/slice-34.json` backfilled | Slice 34 COMPLETE lacked evidence stub; created with `inferred: true` | AUTO-FIXED |
| 89 | 2026-07-26 | health-check | Gap 8 USER-CONFIRMED: Specification coverage line added to `SLICE-32B` After-Checks | Branch + Mutation already present; added Specification coverage to match craft gate | USER-CONFIRMED |
| 90 | 2026-07-26 | 35 | Postgres/Supabase mode must not show Atlas/Mongo host terminology | Operators on `--postgres` / supabase configs still see "Atlas host", Mongo stall copy, and Atlas-only comments under `configs/supabase/` | Leave to Slice 36 storage_mode polish — delays visible trust while sparse/hybrid land |
| 91 | 2026-07-26 | continuation | Path A Resume Slice 35 | User confirmed Gap 8 y + Resume A; TRAIL/PROGRESS/SLICE-35 → 🔨 IN PROGRESS; includes sparse/hybrid + Supabase-mode copy hygiene (#90) | PLAN HEALTH → RESUMED |
| 92 | 2026-07-26 | 35 | No `sparsevec` column; keyword sparse via tsvector; SPLADE embed storage stays blocked | Gate requires measured non-zeros before locking sparsevec; BM25-equivalent sparse does not need SPLADE vectors | Add sparsevec without measurement |
| 93 | 2026-07-26 | 35 | Equivalence vs Mongo CONDITIONAL — document Lucene/`ts_rank` drift; full dual-backend matrix → Slice 38 | Slice 38 owns side-by-side quality artifact; engine drift is known and acceptable for this skateboard | Block 35 on live dual-backend overlap ≥80% |
| 94 | 2026-07-26 | 35 | Hybrid RRF in Python SQL CTEs (`rrf_k=60`); weights as kwargs; YAML tunables N/A | Dual-width columns need dynamic vector Identifier; Mongo also uses k=60 with no config weights | Named plpgsql function per width; expose YAML knobs early |
| 95 | 2026-07-26 | 35 | Slice 35 verify-slice → COMPLETE; backend mutation waived to nightly CI | No local mutmut/cosmic-ray runner; quality gates 11/11 and full GWT coverage on live pgvector make the slice feature-complete; nightly Stryker carries mutation signal (#33/#80 precedent) | Block COMPLETE on a local backend mutation run that no tooling supports |
| 96 | 2026-07-26 | health-check | Gaps 1–7 OK; Gap 8 pending for SLICE-44 (SLICE-43 COMPLETE skipped per #79) | Formal 8-check audit before Slice 36 resume; TRAIL was missing 43/44 rows | PENDING user on Gap 8 |
| 97 | 2026-07-26 | continuation | Sync Slice 36 vs post-35/43 main | Baseline table + ownership vs 37/38/43; TRAIL rows 43/44; Before-Checks tick 35/43; tokens still `local-postgres`/`supabase` in code | Path A Resume 36 after nw-sar review |
| 98 | 2026-07-26 | review | Slice 36 nw-solution-architect-reviewer → CONDITIONAL; remediations applied | Preflight SQL + 422 contract; behavioral GWT; extend `search_index_guard` (no new guard module); create Mongo mode helper; dashboard updates `cluster_tier_type` in place | Ready to branch on user Path A confirm |
| 99 | 2026-07-26 | health-check | Gap 8 USER-CONFIRMED: coverage gates inserted in SLICE-44 | Specification coverage + Branch coverage + Mutation testing lines added to After-Checks | USER-CONFIRMED |
| 100 | 2026-07-26 | continuation | Path A Resume Slice 36 | User confirmed Gap 8 y + Resume A; TRAIL/PROGRESS/SLICE-36 → 🔨 IN PROGRESS; branch `slice/36-postgres-preflight-stats` | PLAN HEALTH → RESUMED |
| 101 | 2026-07-26 | 36 | Slice 36 verify-slice → COMPLETE; backend mutation waived to nightly CI | No local mutmut/cosmic-ray; live Postgres smoke (`storage_mode=postgres-local`) + unit GWT coverage make the slice feature-complete; nightly carries mutation signal (#95/#33/#80) | Block COMPLETE on a local backend mutation run that no tooling supports |
| 102 | 2026-07-26 | 37 | Absorbed unfinished/non-36 leftovers from Slice 36 close into SLICE-37 | Four-flag parse, compose `local-postgres` spelling, `supabase` provider/`vector_db_id` normalize, engine×location docs — not 36 regressions; `configs/supabase/` keep-path this slice | Leave vocabulary drift on closed Slice 36 After-Checks |
| 103 | 2026-07-26 | continuation | Path A Resume Slice 37 after health OK + drift sync | User asked enhanced-flow-planner then start Slice 37; TRAIL next Must is 37; no routing conflict (expired Graphiti “36 next” vs local 36 COMPLETE) | Fresh plan or Replace |
| 104 | 2026-07-26 | 37 | Drift reconciliation vs main @ 7175bea (36/43 merged) | Mark landed foundations (storage_mode, mongo→mongodb, Path A, TLS, supabase twins); Must owns flags/ensure_env/422/profile rename/normalize/vector_db_id/lifecycle; Won't URI aliases + new example-cloud.yaml; Should CLI/health pause/boot tests; bare start must resolve STORAGE_BACKEND | Keep stale “bare start already works” / “ready when 36 merges” |
| 105 | 2026-07-26 | review | nw-platform-architect-reviewer → CONDITIONALLY APPROVED (planning) | Reviewer scored unimplemented Musts as plan BLOCKERs; reclassified as execution exit criteria; applied concrete 422 + post-start templates; hosted smoke remains optional skip | Treat NEEDS REVISION as plan rewrite / block Path A |
| 106 | 2026-07-26 | quality-lens | Slice 37 9/10 provisional (check 4 N/A — walking skeleton already proven by 33–36) | Fewest elements after Won't URI aliases; GWT-first; composable mode resolver before 422; no speculative IndexBackend; artifact paths under docs/plan/slices | Pending user confirm on adversarial deferrals |
| 103 | 2026-07-26 | 37 | Product-wording contract + code-clarity Shoulds parked on SLICE-37 | Map `storage_mode` → Atlas cloud / Atlas Local / Supabase-hosted Postgres / local pgvector; Must write `configuration.md` axes subsection; Should rename Atlas-only `collect_search_index_snapshot` | Rely on chat memory for operator vocabulary; leave Atlas-ambiguous prose |
| 107 | 2026-07-26 | 37 | Added `SUPABASE_URI` as optional alias for `DATABASE_URL` (reversing the earlier Won't) after user request; ran a real hosted Supabase smoke | User has a live Supabase project — a product-named alias lowers switching friction and the hosted end-to-end run (exp `49c23d41`, `storage_mode=postgres-cloud`) upgrades the documented skip to a verified pass; `DATABASE_URL` stays canonical and wins when both are set | Keep Won't URI aliases / leave hosted path as documented skip |
| 108 | 2026-07-26 | 37 | Remove CLI flag aliases `--local`/`-l`/`--postgres`/`-p` (hard fail as unknown options); keep env `RAG_LOCAL_ATLAS`/`RAG_LOCAL_POSTGRES` with deprecation notice | Operator requested ambiguous short flags disallowed; four-flag vocabulary is the only CLI path; env aliases retained for scripts that still set them | Keep soft deprecation notices for the short flags; remove env aliases too |
| 109 | 2026-07-26 | 37 | Keep generic `Unknown option` for removed flags (do not name the replacement) | Operator explicitly chose generic unknown-option DX; `--help` + docs carry the canonical four flags | Platform-review suggestion to print `use --mongodb-local instead` |
| 110 | 2026-07-26 | 37 | Gate root `start-services.sh` with pre-commit/CI shellcheck (pattern includes `start-services.sh` and `scripts/**/*.sh`) | Large Slice 37 rewrite of the operator entrypoint was outside the old `scripts/*.sh` glob | Leave root script ungated / silence SC2155 without fixing |
| 111 | 2026-07-26 | health-check | Gaps 1–4, 6–8 OK; Gap 5 deferred (32/32B/32C/33 not COMPLETE so no stub); SLICE-38 already has coverage gates | Formal 8-check before Slice 38; no AUTO-FIX applied | Gap 5 SKIPPED — tracker debt not PASSED |
| 112 | 2026-07-26 | continuation | Sync SLICE-38 vs post-35/36/37/43 main | Already-landed table; GWT for latency/CI; rollback uses `mongodb` + two-command flags; Before-Checks = 34–37+43 evidence not formal 32 COMPLETE; non-goals exclude 32B/44 | Keep pre-37 stub that still said `STORAGE_BACKEND=mongo` and optional flag-alias removal |
| 113 | 2026-07-26 | continuation | Path A intent = Resume Slice 38 after nw-platform-architect-reviewer | User asked enhanced-flow-planner sync + nw review then start 38; next Must from HANDOFF | Fresh plan / Replace / start 32B first |
| 114 | 2026-07-26 | 38 | Latency cutover gate = QUERYING-phase `elapsed_ms` median+max (≤2×), not p99 | No per-query percentile instrumentation exists; inventing a probe exceeds 3–4 h docs slice; phase elapsed is already recorded | Build a p99 probe harness in Slice 38 |
| 115 | 2026-07-26 | 38 | Authoritative comparison baseline = Slice 43 shape (mirrored local 384-dim configs), both sides measured fresh | ADR-003 has no baseline p99; PRD 36×1000×1024 Voyage shape infeasible at free-tier RPM in slice budget | Require Voyage 36×1000×1024 as hard Before-Check |
| 116 | 2026-07-26 | 38 | Default flip must update settings.py + storage_mode.sh + docker-compose.yml; Mongo branch exports `STORAGE_BACKEND=mongodb` | Asymmetric export leaves hostile leftover `STORAGE_BACKEND=postgres` on Postgres after `--mongodb-*` | Flip settings.py only |
| 117 | 2026-07-26 | 38 | Comment out `.env.example` `SUPABASE_URI` placeholder; reject `<project-ref>` Postgres placeholders | Uncommented placeholder passes both URI guards and breaks fresh clone after default→postgres | Leave placeholder uncommented |
| 118 | 2026-07-26 | review | nw-platform-architect-reviewer Slice 38 planning → NEEDS REVISION; remediations 1–8 applied to SLICE-38 | BLOCKERs: flip surfaces/rollback export, latency metric, baseline feasibility, SUPABASE_URI placeholder; CONDITIONAL floor ≥50% + sign-off | Treat NEEDS REVISION as replan / ignore rollback asymmetry |
| 119 | 2026-07-26 | 38 | Path A Resume — landed review remediations in code before any default change | `export_storage_backend_for_stack`; Mongo compose + `apply_stack_profiles` export `mongodb`; reject `<project-ref>` placeholders in shell + settings; comment out `.env.example` SUPABASE_URI; do not silently change code default without an explicit cutover decision | Flip default to postgres before remediations land / without recording a decision — **fail-safe reading SUPERSEDED by #129** |
| 120 | 2026-07-26 | 38 | Pin Atlas Local `8.3.3` + pgvector `0.8.5-pg16` | `8.0.9` failed against FCV 8.3 volumes (exit 62); `:latest` worked but less reproducible; `8.3.3` matches volume FCV and pins the patch | Keep `:latest` forever / pin digests only |
| 121 | 2026-07-26 | 38 | After image-tag / FCV churn: recreate containers; if healthy but `NotPrimaryOrSecondary` / invalid RS → `mongodb reset` + start + restart server | Ping-only healthcheck can pass while replica set is not writable; POST /experiments then 500s | Treat `healthy` alone as sufficient for submit |
| 122 | 2026-07-26 | 38 | Postgres container ops parity with Mongo: shared `wait_for_postgres_local_healthy`, `postgres reset` hints, NONINTERACTIVE `:5433` conflict messaging | Mongo had extracted wait + reset UX; Postgres wait was inlined and hinted raw `docker rm` | Leave Postgres lifecycle UX thinner than Mongo |
| 123 | 2026-07-26 | 38 | `scripts/health-check.sh` probes active `/healthz` backend **and** any present Atlas Local / pgvector containers | Previously only reported Mongo when `storage_backend=mongodb`, skipping a running Postgres container on the same laptop | Probe only the active backend |
| 124 | 2026-07-26 | 38 | sync-docs: Mongo↔Postgres operator/doc parity for container lifecycle | Close Path D / native-dev / ops-table / troubleshooting / local-environment / architecture / CLAUDE / SIE footer drift vs #122–#123 | Leave Mongo-biased docs until cutover |
| 125 | 2026-07-26 | 38→43 | Park unrealistic Slice 38 gates on Slice 43 residuals | Hosted production-claim matrix, PRD §9/§docs historical tick, 100% shell coverage, Graphiti/date spelling as COMPLETE gates — keep 38 on local comparison + ADR + explicit default decision (#129) | Keep hosted matrix as Slice 38 COMPLETE blocker |
| 126 | 2026-07-26 | 38→43 | Park **all** non-100%-Yes Slice 38 gate items on Slice 43 | Pro-tier ADR mandate, tracker one-liners, `/sync-docs`/doc-matrix, shell-exclusion docs, spec-coverage After-Check — 38 After-Checks = local comparison + ADR + no-flip (#130) + slice-38.json + mutation waive + PROGRESS/TRAIL/CHANGELOG only. Post-flip GWTs dropped by #130 | Leave nuanced items as Slice 38 COMPLETE blockers |
| 127 | 2026-07-26 | 38 | ADR-004 Accepted — dual-backend Postgres/pgvector (Supabase) + Mongo; ADR-003 Superseded | Endorse first-class Postgres/Supabase **in addition to** Mongo; code default stays `mongodb` permanently (#130) | Treat ADR accept as requiring a postgres code default |
| 128 | 2026-07-26 | 38 | Slice 38 backend mutation waived to nightly CI | No local mutmut/cosmic-ray; Slice 38 lands ADR + ops remediations + gate-evidence (no non-trivial new Python retrieval/storage logic this close-out); nightly carries mutation signal (#95/#101/#33/#80) | Block COMPLETE on a local backend mutation run that no tooling supports |
| 129 | 2026-07-26 | 38 | Mongo and Postgres are **independent** backends — neither is a fail-safe for the other | Dual-backend = operator chooses engine via flags/env; comparison artifact is quality/latency evidence for that choice; “fail-closed keep mongodb” must not be read as Mongo protecting Postgres (or vice versa). Supersedes #119’s fail-safe reading; #119 still means “do not change code default without an explicit decision” | Treat Mongo as automatic failover / safety net for Postgres |
| 130 | 2026-07-26 | 38 | **Won't** — no `STORAGE_BACKEND` default flip; code default stays `mongodb` permanently | Dual-backend is operator choice via flags/env (#129); a postgres code default was never a product goal. Supersedes any “deferred / optional flip” reading of Slice 38 close-out | Keep a default-flip gate, residual, or future flip commitment |
| 131 | 2026-07-26 | health-check | Gaps 1–7 OK; Gap 8 SKIPPED for COMPLETE historical stubs (active PLANNED stubs incl. 44 already have Specification coverage gates) | Continuation audit before Slice 44 migrate; no AUTO-FIX | Insert coverage gates into COMPLETE historical stubs |
| 132 | 2026-07-26 | continuation | Path B revise — migrate SLICE-44 to latest plan-generator stub + live code baseline | User passed SLICE-44 + current code into enhanced-flow-planner; measured `npm run test:coverage` → lines 50.18% / stmts 48.48%; gates still bare `npm run test` / CI `verify`; floor = re-measure at Before-Check; whole-tree 100% branch Won't | Fresh plan / Replace / leave pre-baseline stub |
| 133 | 2026-07-26 | quality-lens | Slice 44 provisional 9/10 (check 4 N/A — not first walking skeleton of the plan) | Must = embed coverage + honest floor; Should = module tests; no speculative framework; GWT-first; composable with no deps; artifact path `docs/plan/slices/SLICE-44-…` | Pending user confirm on adversarial questions |
| 134 | 2026-07-26 | sync-docs | Post-planning audit for Slice 44 migrate | Fixed `development.md` frontend **15→16** tests; backend unit baseline **268→322** (CLAUDE **317→322**); noted coverage scripts exist but ungated (**PROPOSED** Slice 44); refreshed HANDOFF + Forward Roadmap row 44 | Claim coverage floor as current capability |
| 135 | 2026-07-27 | 44/45 | Structure taxonomy audit (Behavior \| Feature \| Function) lands as Slice 44 Should §3; filesystem moves / import rewrites are Won't for 44 and owned by new Slice 45 Could | Keeps coverage-gate Must primary; audit is hygiene/planning (same slice class); executing moves is a different abstraction (SLAP). Theme labels + five hotspots documented in `module-theme-map.md` + canvas; `SLICE-45-MODULE-THEME-SEPARATION.md` holds move tables | New slice number for the audit alone / execute moves inside 44 |
| 136 | 2026-07-27 | sync-docs | Post-taxonomy sync: CHANGELOG + CLAUDE + development + AGENTS + ARCHITECTURE stub point at theme map; coverage floor remains **PROPOSED**; taxonomy §3 **IMPLEMENTED**; Slice 45 moves **PROPOSED** | Close agent/contributor/release surfaces after Slice 44 §3 artifacts landed; do not claim gated frontend coverage | Leave CHANGELOG/agent entry points silent on theme map |
| 137 | 2026-07-27 | 44/45 | nw-review remediations applied to SLICE-44 + SLICE-45 stubs | Locked gate scripts (`test:coverage` local / `test:ci` CI); usage metrics; §3 vs COMPLETE clarified; Slice 45 Reuse Analysis + re-export lifecycle + blast-radius `rg` + quantified GWT. PO Elevator Pitch already present. Superseding verdicts: 44 DoR APPROVED for execution; 45 architect APPROVED | Leave NEEDS_REVISION / CONDITIONALLY APPROVED without stub updates |
| 138 | 2026-07-27 | 44 | Frontend coverage floor locked then ratcheted: Before-Check baseline lines **50.18%** / branches **47.74%** / functions **45.38%** / statements **48.48%**; after Should tests measured ≈64.75/58.22/61.15/62.63 — thresholds set to **64/58/61/62**. Mutation testing waived (CI-hygiene; no non-trivial new pure logic) | Honest floor from re-measure; ratchet when tests raise coverage so gates catch regressions | Keep baseline 50% floor after Should tests / invent aspirational 100% |
| 141 | 2026-07-27 | coverage | Fair metric breakdown: FE **95/90/95/95**; BE policy **92/85/n/a/90** with enforced `fail_under=90` (combined Cover) | Interim after #140. **Superseded by #142** (BE raised + JSON checker) | Keep flat FE 90 / invent BE branch≥90 without raising measured branch |
| 142 | 2026-07-27 | coverage | Shared product floors **95/90/95/95** for FE; BE **95/90/n/a/95** (functions n/a on coverage.py) | FE Vitest-enforced. BE: gap tests (`test_coverage_floor_gaps.py`) + `fail_under` **90→95** + `scripts/ci/check_backend_coverage_floors.py` (stmts/br/lines from JSON; floors in `[tool.rag_params_finder.coverage_thresholds]`). Wired in quality-gates / pre-push / CI. Measured stmts≈98.6 / br≈95.2 / TOTAL≈97.7. Supersedes #141 soft BE 92/85 | Keep BE fail_under=90 / aim-only without JSON checker |
| 140 | 2026-07-27 | coverage | Uniform product coverage floor **≥90%** for backend and frontend | Backend unit scoped gate `--cov-fail-under` / `fail_under` **80→90** (measured TOTAL ≈93.2%). Frontend Vitest thresholds briefly **90/90/90/90**. Postgres `retriever_postgres` CI module gate stays **≥95%** (specialized). **FE quartet superseded by #141**; BE fail_under=90 retained until #142 | Keep asymmetric FE 95 / BE 80 |
| 139 | 2026-07-27 | 44 | Phase B reopen: FE coverage gate → **≥95%** stmts/funcs/lines + **≥90%** branches (`all: true` on `src/**`; exclude tests/setup/main/types) | User chose option 1 + reopen 44 (not 44B). Surpasses backend `--cov-fail-under=80`. Supersedes #132/#138 “100% Won’t / 64% floor” for product thresholds. **Superseded for product floors by #140** | Keep 64% floor / invent literal 100% without exclusions / new Slice 44B |
| 143 | 2026-07-27 | health-check | Gap 8: Specification coverage gates inserted into SLICE-45 After-Checks | Active PLANNED stub lacked §12 coverage / branch / mutation After-Check lines; user confirmed (y); product floors #142 retained (not whole-tree 100%) | SKIPPED — leave Gap 8 open on Slice 45 |
| 144 | 2026-07-27 | continuation | Path A Resume — Slice **45** (not first TRAIL 🔨 32) | User redirected resume to module-theme-separation after Gap 8; 32/32C/32B remain open Must chain; 45 is non-blocking Could hygiene | Resume 32 only / leave 45 PLANNED |
| 145 | 2026-07-27 | review | nw-solution-architect-reviewer Slice 45 roadmap iter2 → **APPROVED** (0 blocking) | Post–Gap 8 + resume; #137 remediations still hold; Before-Checks live count **1/10** (orchestrator corrected reviewer’s 8/11 claim) | NEEDS_REVISION / re-open Must move tables |
| 146 | 2026-07-27 | 45 | Phase 1 skateboard = `core/guards/` then `pipeline/` orchestrator SLAP (not whole `core/` in one PR) | Blast-radius core ≈112 match-lines (&lt;200); CLI touches `search_index_*`; highest cognitive load still orchestrator — sequence guards first for safer import rewrite | Move entire `server/core/` in one PR / FE-primitives-first |
| 147 | 2026-07-27 | 45 | Landed `server/core/guards/` + shim re-exports at old `server.core.*` paths; test patches retargeted to `server.core.guards.*` | Preserve caller imports for one release; mypy-safe explicit re-exports; coverage cov paths → guards | sys.modules alias shims / rewrite all callers in same PR |
| 148 | 2026-07-27 | 45 | Land `server/core/pipeline/` (orchestrator/executors/experiment_control/startup_reconciliation); `sys.modules` shims for patch compatibility | Mega-suite patches `server.core.orchestrator.*`; function-level SLAP deferred to next PR on same branch | Split `_run_single` / bayesian in same commit as package move |
| 149 | 2026-07-28 | 45 | Orchestrator SLAP: extract `pipeline/search.py` + `signatures.py`; keep `_update_phase` / final-status / Bayesian in `orchestrator.py` | Status helpers close over `get_storage_backend` in defining module — extracting them breaks `patch("server.core.orchestrator.get_storage_backend")`; search/signature SUTs retarget patches narrowly | Extract status helpers without patch retargeting / rewrite all mega-suite patches in one go |
| 150 | 2026-07-28 | 45 | Land `server/core/embedding/` + `server/core/retrieval/`; `sys.modules` shims at old flat paths | Same patch-compat pattern as pipeline (#148); factory tests retarget `sys.modules` keys to `embedding.*`; rerank deferred to #151 | Move rerank in same commit / rewrite all callers without shims |
| 151 | 2026-07-28 | 45 | Land `server/core/rerank/` (`reranker`, `local_reranker`); `sys.modules` shims at old paths | Completes core Feature provider cluster from theme map; search imports canonical `server.core.rerank.reranker` | Fold into #150 / leave flat |
| 152 | 2026-07-28 | 45 | Land `server/db/{ports,mongo,postgres}/`; shims at old flat `server.db.*` paths except **no** `server/db/postgres.py` (collides with package); pool API re-exported on `server.db.postgres` package; `schema.sql` co-located with `postgres.py` | Must hotspot #2; preserves Protocols + factory; `from server.db import postgres` / `close_pool` stays valid | Split db across multiple PRs / invent `pg/` package rename |
| 153 | 2026-07-28 | 45 | Fat-surface slim: `experiments_lifecycle.py` + `cli/display.py`; keep routers/commands as façades | Execution order step 3 after db; pre→post lines experiments 553→388, cli/main 519→246 | Defer slim until FE primitives / leave god modules |
| 157 | 2026-07-28 | 45 | Split `frontend/src/components/` into `screens/`, `chrome/`, `experiment/`, `stats/` (+ co-located tests); nest `experimentDetail` under `experiment/` | Step 7 after SLAP; App + mocks retargeted; FE floors still green | Leave flat components/ / move without co-located tests |
| 158 | 2026-07-28 | 45 | FE shared test helpers under `frontend/src/test/helpers/` (`experiments`, `vectorDbStats`, `explore`, `experimentDetail`); screen + stats suites import builders | Step 8 After-Check; `src/test/**` already coverage-excluded; 261 tests / floors unchanged | Leave duplicated builders in mega-suites / invent factories only for new tests |
| 159 | 2026-07-28 | 45 | Land `scripts/{ci,docker,release,security}/`; keep `lib/`; thin shims at old `scripts/<name>` for one minor | Completes hotspot #5 Could; living docs/CI/pre-commit use canonical paths; historical slice specs left as snapshots | Big-bang delete old paths without shims / leave scripts flat |
