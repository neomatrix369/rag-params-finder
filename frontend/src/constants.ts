/** Polling interval for the experiments list screen (ms). */
export const EXPERIMENTS_POLL_MS = 500;

/** Polling interval for the experiment detail screen (ms). */
export const DETAIL_POLL_MS = 2000;
