"""Storage/retriever ports and factory — Function theme (Slice 45).

Import from submodules (e.g. ``server.db.ports.store_factory``).
Temporary shims remain at ``server.db.<module>`` for one release.
"""
