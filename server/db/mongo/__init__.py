"""MongoDB Atlas / Atlas Local adapters — Behavior | Feature (Slice 45).

Import from submodules (e.g. ``server.db.mongo.mongo_store``).
Temporary shims remain at ``server.db.<module>`` for one release.
"""
