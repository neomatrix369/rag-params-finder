"""Preflight / health guards — Behavior theme (Slice 45).

Import from submodules (e.g. ``server.core.guards.search_index_plan``).
Temporary shims remain at ``server.core.<module>`` for one release.
"""
